<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Bienvenidos</title>
        <link rel="icon" href="images/conex.png">
        <link rel="stylesheet" href="/css/styles.css">
        <link rel="stylesheet" href="/css/form.css">
        <link rel="stylesheet" href="static/ass/styles">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
        <script src="https://kit.fontawesome.com/2da8e91210.js" crossorigin="anonymous"></script>
    </head>
    <body>
        <header class="header">
            <nav>
              <input type="checkbox" id="check">
              <label for="check" class="checkbtn">
                  <i class="fas fa-bars"></i>
              </label>
              <a href="#" class="enlace">
                  <img src="images/conex.png" alt="" class="logo">
              </a>
              <ul>
                <h2 class="dashboard-title">Detalle del usuario</h2>
                  <!-- <li><a class="active" href="/">Inicio</a></li> -->
                  <% if (locals.isLogged) { %> 
                    <a href="/users/profile">HOLA <%= locals.userLogged.name %>!</a>
                   <% } %>                  
              </ul>
          </nav>
            </header>
        <form class="form" method="POST" action="/edit/<%= users.id %>" enctype="multipart/form-data">
    
            <h1>Editar usuario</h1>
            <div class="form-container">
                
                <div class="input-container">
                <i class="fas fa-user icon" ></i>
                <input type="text" name="full_name" placeholder="Nombre completo" value="<%= locals.oldData ? oldData.full_name : users.full_name %>">
                </div>
                <small style="color: red;" > <% if (locals.errors && errors.full_name) { %>
                    <%= errors.full_name.msg %>
                    <% } %>
                </small>
                <div class="input-container">
                <i class="fa-solid fa-phone icon"></i>
                <input type="tel" name="phone_number" placeholder="Teléfono" value="<%= locals.oldData ? oldData.phone_number : users.phone_number %>">
                </div>
                <small style="color: red;" > <% if (locals.errors && errors.phone_number) { %>
                    <%= errors.phone_number.msg %>
                    <% } %>
                </small>
    
                <div class="input-container">
                <i class="fas fa-envelope icon"></i>
                <input type="text" name="email" placeholder="Correo electrónico" value="<%= locals.oldData ? oldData.email : users.email %>">
                </div>
                <small style="color: red;" > <% if (locals.errors && errors.email) { %>
                    <%= errors.email.msg %>
                    <% } %>
                </small>
    
                <div class="input-container">
                <i class="fas fa-key icon"></i>
                <input type="password" name="password" placeholder="Contraseña" value="<%= locals.oldData ? oldData.password : users.password %>">
                </div>
                <small style="color: red;" > <% if (locals.errors && errors.password) { %>
                    <%= errors.password.msg %>
                    <% } %>
                </small>
            
            <div class="profile-image">Elegí una foto de perfil:</label>
            <input type="file" name="avatar" id="profile-mage" value="<%= locals.oldData ? oldData.avatar : users.avatar %>">
                </div>
                <small style="color: red;" > <% if (locals.errors && errors.avatar) { %>
                    <%= errors.avatar.msg %>
                    <% } %>
                </small>
                <br>
                <div class="profile">
                    <h4 class="user_profile">Perfil del usuario</h4>
                    <br>
                        <input type="radio" name="role" value="user" id="user">
                        <label for="user">Usuario</label>
                        <input type="radio" name="role" value="admin" id="admin">
                        <label for="admin">Administrador</label><br>
                        <small style="color: red;"> <% if (locals.errors && errors.role) { %>
                            <%= errors.role.msg %>
                        <% } %> 
                        </small>
                </div>
                <br>
                 <input type="submit" value="Actualizar" class="button">
                 <br>
                 <br>             
            </div>
        </form>
    </body>
</html>